{
    "require": {
        "php": ">= 8.2",
        "ext-json": "*",
        "symfony/console": "^6.3",
        "symfony/finder": "^6.3",
        "symfony/process": "^6.3"
    },
    "require-dev": {
        "roave/security-advisories": "dev-latest",
        "symfony/var-dumper": "^6.3"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
